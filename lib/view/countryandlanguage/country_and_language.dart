import 'package:flutter/material.dart';

class CountryAndLanguage extends StatelessWidget {
  const CountryAndLanguage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}